# Salary Management — client

Minimal React + TypeScript client scaffold for the assessment.

Run tests:

```bash
cd client
npm install
npm test
```

Run dev server (Vite):

```bash
cd client
npm install
npm run dev
# open http://localhost:5173
```

Build for production:

```bash
npm run build
npm run preview
```
