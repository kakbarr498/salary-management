# Salary Management - Server

Quickstart:

- cd server
- npm install
- npm run dev (starts the server)
- npm run seed (seeds 10k employees)
- npm test (runs unit tests)

Notes:
- Database is a local SQLite file at server/data/salary.db
- Seed uses server/data/first_names.txt and last_names.txt to generate names
